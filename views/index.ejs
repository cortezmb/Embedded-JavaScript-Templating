<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <% if (pic.length > 0) { %>
     
        <% for( let i = 0; i < pic.length; i++ ) { %>
            <img src="pic[i]" height = "300px" width="100px" alt="Images">
        <% } %>
    <% } %>
   
</body>
</html>